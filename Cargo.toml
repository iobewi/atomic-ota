[package]
edition = "2024"
name = "fibewi"
rust-version = "1.88"
version = "0.1.0"
license = "MIT"
description = "Transactional firmware update core with resumable writes and restart-safe reconciliation."
repository = "https://github.com/iobewi/fibewi"
readme = "README.md"

[workspace]
members = [".", "backends/esp"]
resolver = "3"

[dependencies]
sha2 = { version = "0.10", default-features = false }

[dev-dependencies]
